#!/usr/bin/python3


class Square:
    def __init__(self, size=0):
        for x in size
            if x != int
                raise TypeError(size must be an integer)
            if x < 0 
                raise ValueError(size must be >= 0)
            self.__size = size

    def area(self)
        return self.size ** 2
